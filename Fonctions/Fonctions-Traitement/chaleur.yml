version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  chaleur:
    lang: golang-middleware
    handler: ./chaleur
    image: myceliumir/chaleur:latest
    annotations:
      topic: fonction_chaleur
    environment:
      MORE_DATA: false
